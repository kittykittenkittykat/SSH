# Level 8

- sort data.txt | uniq -u

uniq 명령어부터 써주고싶은데, 인접하고 중복된 애들만 처리해줍니다.
그래서 sort로 data.txt 내용들을 알파벳순으로 정렬해줍니다.
이때 중복되는 내용들도 인접하게 출력해줍니다.
근데 data.txt파일 내용 자체를 바꾸는것이 아니므로 출력값(정렬된 값)을 |를 이용해 바로 uniq -u로 넘겨줍니다.
이때 -u는 unique의 약자로 중복되지 않는, 말그대로 unique한 내용을 출력해줍니다.

이렇게 비밀번호를 알아냈습니다.
